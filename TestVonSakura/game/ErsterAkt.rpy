label test00000:
    warning "This Label is only a testlabel to go outside the script"

    "If you see this message, the jump outside the script function is good"
    jump akt00
label test00001:
    warning "This Label is only a testlabel to go outside the script. 2nd"

    "If you see this message, the jump outside the script function is good. and its still working"
    jump akt00